{
  "name": "humanitydao-subgraph",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ protofire/humanityDao",
    "create-local": "graph create --node http://localhost:8020/ protofire/humanity",
    "remove-local": "graph remove --node http://localhost:8020/ protofire/humanity",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 protofire/humanity"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.1"
  }
}
